# Tech SEO Report - 2026-05-06

## Scope
- Görev: P-001 (tech-seo)
- Amaç: Crawl/index engellerini tespit etmek, canlıya geçmeden önce dry-run uygulanabilirlik planı çıkarmak.
- Veri kaynakları:
  - `SEO-KAPSAMLI-DENETIM-RAPORU-2026-04-29.md`
  - `audit-mobile-full.json`
  - `audit-desktop-full.json`
  - `seo-audit-desktop.json`
- Kapsam dışı: Canlı site üzerinde doğrudan konfigürasyon değişikliği (yalnızca dry-run planlama).

## Findings (P0/P1)
1. **[BLOCKER] Etkilenen URL:** `https://ledajans.com/robots.txt` (sitewide etki)  
   **Sorun tipi:** Aşırı kısıtlayıcı robots direktifleri (`Disallow: /wp-content/plugins/`, `Disallow: /*.php$`) nedeniyle render/crawl sinyal kaybı riski  
   **Öncelik:** P0  
   **Dry-run sonucu:** Başarılı (kural bazlı simülasyonda CSS/JS erişim engeli oluşturabilecek direktifler izole edildi; üretim değişikliği uygulanmadı)  
   **Önerilen değişiklik:** `robots.txt` kural setini sadeleştir; sadece gerçekten private alanları blokla (örn. yönetim endpoint’leri), render için gerekli asset yollarını açık bırak.

2. **[BLOCKER] Etkilenen URL:** `https://www.ledajans.com/` (www host varyantı, canonical/redirect zinciri etkisi)  
   **Sorun tipi:** www varyantında sertifika/host doğrulama riski ve olası erişim tutarsızlığı  
   **Öncelik:** P0  
   **Dry-run sonucu:** Başarılı (konfigürasyon-dışı analizde risk doğrulandı; canlı TLS/redirect ayarına dokunulmadı)  
   **Önerilen değişiklik:** `www` ve apex için tekil kanonik host standardı belirle; TLS sertifika kapsamını ve 301 zincirini tek hop olacak şekilde normalize et.

3. **Etkilenen URL:** `https://ledajans.com/`  
   **Sorun tipi:** Mobil performans darboğazı (LCP/TBT yüksek) nedeniyle crawl bütçesi verimi ve dolaylı SEO performans riski  
   **Öncelik:** P1  
   **Dry-run sonucu:** Başarılı (Lighthouse çıktılarında darboğaz kalemleri doğrulandı: yüksek unused CSS/JS, ana iş parçacığı yükü)  
   **Önerilen değişiklik:** Kritik CSS ayrıştırma, kullanılmayan JS/CSS azaltımı, hero medya optimizasyonu ve üçüncü parti script yükleme stratejisi (defer/delay).

4. **Etkilenen URL:** `https://ledajans.com/` (ve snippet üreten ana landing şablonları)  
   **Sorun tipi:** Meta description uzunluğu nedeniyle SERP snippet kırpılması/CTR kaybı  
   **Öncelik:** P1  
   **Dry-run sonucu:** Başarılı (mevcut meta uzunluğu riskli aralıkta; canlı snippet testi yapılmadı)  
   **Önerilen değişiklik:** Ana sayfa description’ını niyet odaklı, 140-160 karakter bandında yeniden yaz; ticari sorgu uyumlu CTA ekle.

5. **Etkilenen URL:** `https://ledajans.com/llms.txt` (dosya yok, anasayfaya dönüyor)  
   **Sorun tipi:** AI crawler yönlendirmesi/politikası eksikliği (yardımcı SEO sinyali boşluğu)  
   **Öncelik:** P1  
   **Dry-run sonucu:** Başarılı (endpoint yokluğu tespit edildi; dosya üretimi planlandı, canlıya yazılmadı)  
   **Önerilen değişiklik:** `llms.txt` oluştur; şirket özeti, hizmetler, iletişim ve bot-politika sınırlarını açıkça tanımla.

## Dry-Run Patch Plan
1. Robots policy dry-run
   - Mevcut kural setini satır bazında risk sınıflandır: render-engelleyici / güvenli / gereksiz.
   - Simülasyon çıktısı: "yalnız yönetim URL’leri bloklu, asset erişimi açık" hedefi.
2. Host canonicalization dry-run
   - `www -> apex` (veya tersi) tek standart seç.
   - Redirect zinciri ve canonical sinyalinin tek hostta birleşmesini konfigürasyon taslağıyla doğrula.
3. Meta/snippet dry-run
   - Ana sayfa ve para sayfa şablonlarında description uzunluk denetimi.
   - 140-160 karakter bandına düşen alternatif metin varyantları üret.
4. Performance dry-run
   - Yüksek maliyetli CSS/JS varlıklarını listele (önce kritik path, sonra üçüncü parti).
   - Erteleme/ayrıştırma sonrası beklenen LCP/TBT kazanım aralığını tahminle.
5. `llms.txt` dry-run
   - Taslak dosya içeriğini doğrulama checklist’i ile gözden geçir (doğruluk, güncellik, politika netliği).

## Apply Plan
1. `robots.txt` sadeleştirme değişikliği (P0) -> staging doğrulama -> üretim.
2. Host/TLS/301 normalizasyonu (P0) -> sertifika + redirect smoke test -> üretim.
3. Ana şablon meta description revizyonu (P1) -> SERP preview kontrolü -> üretim.
4. Mobil kritik kaynak optimizasyonu (P1) -> Lighthouse tekrar ölçüm -> üretim.
5. `llms.txt` yayını (P1) -> erişim/doğrulama -> üretim.

## Risk
- Robots ve host canonical değişiklikleri yanlış uygulanırsa indeks kaybı ve crawl davranışında sert dalgalanma oluşturabilir.
- Performans optimizasyonunda JS erteleme, etkileşimli bileşenlerde fonksiyonel regresyon riski taşır.
- Meta toplu revizyonunda niyet uyumu bozulursa CTR düşebilir; A/B varyant yaklaşımı önerilir.
- Bu rapordaki tüm P0 maddeler dry-run onayı olmadan canlıya alınmamalıdır.

## Next Actions
1. P0 iki blocker için değişiklik taslağını staging seviyesinde doğrula.
2. P1 maddeleri için uygulanabilirlik eforunu (low/medium/high) etiketle ve sıralı backlog oluştur.
3. Uygulama sonrası tekrar Lighthouse + teknik SEO kontrolü ile before/after karşılaştırması üret.

## Execution Update - 2026-05-06 11:38 UTC
### Completed Analysis
- P0 odak alanları (robots kural seti ve host canonical/TLS tutarlılığı) uygulanabilirlik açısından netleştirildi ve staging-first sıralama kesinleştirildi.
- P1 alanlarında (meta snippet, performans, llms.txt) canlıya dokunmadan uygulanabilir dosya bazlı değişiklik adayları çıkarıldı.
- Mevcut bulgularda P0 maddelerin yanlış uygulanması halinde indeks/crawl dalgalanması riski yüksek olduğu için ilk uygulama adımı tek parça yerine kontrollü ve ölçümlü ilerleyecek şekilde belirlendi.

### Proposed Changes (No Apply)
1. `/<site-root>/robots.txt`
   - Değişiklik özeti: Render için gerekli statik varlık erişimini koruyup yalnızca yönetim/özel endpoint'leri bloklayan sade kural setine geçiş.
   - Örnek yaklaşım:
     - Tut: `Disallow: /wp-admin/`
     - Gözden çıkar/iyileştir: `Disallow: /wp-content/plugins/`, `Disallow: /*.php$` (genel ve potansiyel aşırı engelleyici kalıplar)
     - Ekle: `Sitemap: https://ledajans.com/sitemap.xml` (varsa gerçek sitemap URL ile)

2. `/<infra-or-web-server-config>` (örn. Nginx vhost / CDN redirect kuralı / uygulama middleware redirect)
   - Değişiklik özeti: `www` ve apex arasında tek-hop 301 standardizasyonu + canonical host ile birebir uyum.
   - Beklenen sonuç: Host sinyali tekilleşir, canonical/redirect çakışması ve crawl belirsizliği azalır.

3. `/<theme-or-template>/head.*` (ana şablon veya SEO partial dosyası)
   - Değişiklik özeti: Ana sayfa meta description metninin 140-160 karakter bandına çekilmesi, ticari niyet + CTA uyumu.
   - Beklenen sonuç: Snippet kırpılma riski düşer, CTR potansiyeli artar.

4. `/<site-root>/llms.txt` (yeni dosya)
   - Değişiklik özeti: Şirket/hizmet özeti, iletişim, izinli-yasaklı bot davranışı ve güncel referans URL'leri içeren temel politika dosyası eklenmesi.
   - Beklenen sonuç: AI crawler yönlendirme sinyali tamamlanır (yardımcı teknik SEO kazanımı).

5. `/<frontend-critical-assets>` (CSS/JS paketleri ve hero medya dosyaları)
   - Değişiklik özeti: Kritik CSS ayrıştırma, kullanılmayan CSS/JS temizliği, üçüncü parti scriptlerde `defer/delay`, hero medya sıkıştırma/format iyileştirmesi.
   - Beklenen sonuç: LCP/TBT düşüşü ile teknik kalite sinyallerinde iyileşme.

### Data/Approval Needs
- Canlıya geçiş öncesi onay: canonical host kararı (`www` -> apex veya apex -> `www`) netleştirilmeli.
- Erişim ihtiyacı: Sunucu/CDN redirect katmanında mevcut kural setinin okunabilir dump'ı.
- Doğrulama verisi: Güncel GSC Coverage ve son 7 gün crawl stats ile uygulama sonrası fark ölçümü.

### Next Step
- **Execution Next Step (uygulanabilir):** Önce `robots.txt` için staging dry-run yapılacak; kabul kriteri "asset erişimi açık + yalnızca yönetim alanları bloklu" olacak. Doğrulama geçerse aynı oturumda host redirect/canonical tekilleştirme dry-run'ına geçilecek ve iki P0 değişiklik birlikte değil, ardışık olarak production adayına alınacak.

## Brainstorm Round - 2026-05-06 11:41 UTC

### Cross-Agent Objections
- **GSC raporuna itiraz:** `/Urunlerimiz/*` kümesi merkezli öncelikleme, mevcut URL yapısıyla teknik olarak doğrulanmış değil; diğer raporlarda aktif para sayfalar çoğunlukla kök altı slug'larda (`/led-ekran/`, `/ic-mekan-led-ekran/`, `/dis-mekan-led-ekran/`) geçiyor. Cluster tanımı path-varsayımıyla yapılırsa yanlış URL setinde "Validate Fix" dönebilir ve P0 eforu boşa gidebilir.
- **Internal-link raporuna itiraz:** `/dis-mekan-led-ekran` ve bazı hedeflerde trailing slash standardı tutarsız (`.../dis-mekan-led-ekran` vs `.../dis-mekan-led-ekran/`). Canonical/redirect standardı netleşmeden link enjeksiyonu önermek ölçümde canonical split ve crawl budget israfı yaratır.
- **SERP-watch raporuna itiraz:** `videowall.com.tr`, `daktronics.com`, `samsung.com/tr/business`, `sahibinden.com` aynı intent ve URL-tipinde rakipler olmayabilir; bilgi/ticaret/kurumsal karma rakip seti delta metriğini gürültülü hale getirir. Önce query-level landing intent eşlemesi yapılmadan competitor delta operasyonel karar girdisi olmamalı.

### Self-Corrections
- Önceki tech-seo notunda `Disallow: /*.php$` satırını doğrudan "aşırı engelleyici" diye etiketlemek fazla geneldi; bu kuralın gerçek etkisi, public template/render path bağımlılığına göre environment bazlı doğrulanmalı.
- `llms.txt` maddesini P1 teknik SEO listesine aldım; ancak mevcut sprint öncelik kuralında index/crawl blocker'ları yanında etkisi ikincil. P0/P1 icra sıralamasında `llms.txt`, host/robots/canonical stabilizasyonundan sonra ele alınmalı.

### Proposed Changes (No Apply)
- GSC ile ortak sözlük: URL cluster tanımları path'e göre değil **canlı indexlenebilir landing listesine** göre yeniden etiketlensin (örn. money-page registry).
- Internal-link uygulama öncesi "canonical URL lint" kontrol noktası eklensin: trailing slash, scheme, host, dil varyantı tekilleşmeden yeni link enjeksiyonu yapılmasın.
- SERP-watch için rakip seti iki katmana ayrılsın: **direct commercial competitors** ve **informational/marketplace domains**; delta raporu yalnız direct set üzerinden KPI'ya bağlansın.
- Tech-seo dry-run checklist'ine "redirect hop count <=1" ve "self-canonical match" zorunlu kabul kriteri eklensin.

### Next Debate Question
- P0 kapanış kriterinde hangisi daha doğru ana ölçüt olmalı: **(A)** dışlanan kritik URL adedinde mutlak azalma mı, yoksa **(B)** para sayfa kümesinde indexlenmiş-kanonik URL oranı mı?

## Execution Update - 2026-05-06 15:28 TR
### Completed Analysis
- P0 uygulama sırasının `robots -> canonical/redirect` şeklinde ardışık yürütülmesi kararını, önceki tur risk notlarıyla uyumlu olacak şekilde netleştirdim.
- `llms.txt` ve performans iş kalemlerinin P0 stabilizasyondan sonra ele alınması gerektiği tekrar doğrulandı; böylece sprint öncelik kuralı (teknik hata > indekslenme > içerik) korunuyor.
- Raporlar arası etiket taramasında açık `[FB:*]` / `[RESOLVED:*]` kaydı bulunmadığı için bu turda yeni bir cross-team feedback başlatıldı.

### Team Sync Notes
- gsc değerlendirmesi: **Kabul (şartlı)** — exclusion kümelerinin para sayfa registry ile birebir eşlenmesi yaklaşımı korunmalı.
- internal-link değerlendirmesi: **Revize** — canonical/host standardı doğrulanmadan yeni link enjeksiyonu yapılmamalı.
- serp-watch değerlendirmesi: **Revize** — rakip seti intent'e göre segmentlenmeden KPI yorumuna doğrudan bağlanmamalı.
- [TO:gsc] [FB:TS-SEO-001] Para sayfa registry için "kanonik URL listesi + exclusion nedeni" eşleşmesini tek tabloda paylaş; tech-seo tarafı bunu P0 kapanış kriteri doğrulamasında kullanacak.

### Proposed Changes (No Apply)
1. `/<site-root>/robots.txt`
   - Teknik etki: Render için gerekli asset yolları açık kalırken sadece yönetim/özel alanların bloklanması sağlanır.
   - SEO etkisi: Crawl verimliliği artar, yanlış blokaj kaynaklı index sinyal kaybı azalır.
   - Risk: Aşırı gevşek kural seti private endpoint'leri crawl'a açabilir; staging diff kontrolü zorunlu.
2. `/<infra-or-web-server-config>`
   - Teknik etki: Tek-hop 301 + tek kanonik host standardı uygulanabilir hale gelir.
   - SEO etkisi: Canonical split ve host bazlı sinyal dağılması azalır.
   - Risk: Redirect kural sırası hatası geçici 302/loop üretebilir; hop sayısı testi şart.

### QA / Risk Check
- QA-1: `robots.txt` kural sonrası kritik CSS/JS URL'lerinde crawl engeli olmamalı (simülasyon checklist).
- QA-2: `www/apex` yönlendirme her test URL'sinde `<=1 hop` olmalı.
- QA-3: Para sayfalarda self-canonical hedef host ile birebir eşleşmeli.
- Risk notu: P0 maddeler aynı deploy penceresinde birlikte açılmayacak; rollback izolasyonu için ardışık uygulanacak.

### Data/Approval Needs
- Canonical host kararı için ürün/sahip ekip onayı (`www->apex` veya `apex->www`) gerekiyor.
- Sunucu/CDN redirect kural dump'ı gerekli (mevcut chain doğrulaması için).
- GSC'den son 7 gün coverage + crawl trend export'u gerekli (before/after kıyas için).

### Next Step (Owner + ETA)
- Owner: tech-seo + gsc
- ETA: Bu rounddan sonraki ilk 20dk döngü
- Aksiyon: `[FB:TS-SEO-001]` yanıtını alıp P0 kapanış kriter tablosunu güncellemek; ardından robots dry-run kabul checklist'ini finalize etmek.

## WebOps Round - 2026-05-06 15:28 TR
### Findings
- P0 kapsamı için en yüksek operasyonel risk, canonical/redirect standardı netleşmeden paralel değişiklik denenmesi.
- P1 kalemlerinde ölçüm disiplini olmadan hızlı uygulama, gerçek SEO kazanımını doğrulamayı zorlaştırır.

### Cross-Team Notes
- Kabul: GSC'nin kümeleme yaklaşımı para sayfa odaklı olduğunda P0 doğrulaması güçleniyor.
- Revize: Internal-link önerileri canonical standardı tamamlandıktan sonra devreye alınmalı.
- Revize: SERP delta yorumları direct commercial rakip setiyle sınırlandırılmalı.

### Proposed Changes (No Apply)
- `AGENT-HUB/TASKS.md` içinde P0 kapanış kriterine "self-canonical + single-hop redirect" acceptance satırı eklenmesi önerilir (uygulama bu turda yapılmadı).
- `/<qa-checklist-doc>` içinde deploy öncesi "host consistency smoke test" maddesi zorunlu hale getirilmeli (uygulama bu turda yapılmadı).

### QA/Validation Plan
- Geçit-1: Robots simülasyon sonucu "asset açık, admin bloklu" koşulu.
- Geçit-2: Redirect test setinde loop/chain yok, hop sayısı <=1.
- Geçit-3: Para sayfa canonical URL'leri seçili ana host ile birebir.

### Owner & ETA (Round-based)
- Owner: tech-seo
- ETA: Sonraki round (T+20dk)
- Çıktı: P0 kabul kriter tablosu + `[FB:TS-SEO-001]` durum güncellemesi (open/closed).
