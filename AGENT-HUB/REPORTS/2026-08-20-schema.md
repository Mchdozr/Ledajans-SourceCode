# Schema — 2026-08-20

**URL:** https://ledajans.com/dis-mekan-led-ekran/  
**GSC:** Product — `aggregateRating`, `review`, `lowPrice`, `offerCount`, `highPrice` eksik

## Karar (Allow)

**Product kaldır. FAQPage + BreadcrumbList bırak. Service ekleme.**

Aynı kalıp: indoor, rental, GOB hero (`AggregateOffer` `lowPrice`/`highPrice` `"0"`).

## Neden

- Sayfada görünür fiyat yok; `0` TL AggregateOffer Google’da fiyat yok sayılır ve GSC’de “eksik” üretir.
- Sahte `review`/`aggregateRating` yasak; fiyat uydurma yasak (markup = görünür içerik).
- `Service` Google Search rich result tipi değil; header `OfferCatalog` zaten “Dış Mekan LED Ekran” Service içeriyor — sayfa seviyesinde tekrar duplicate entity, SERP kazancı yok.
- FAQPage (`sss.html`) ve BreadcrumbList (`hero-banner.html`) zaten var ve desteklenen rich result.

## GSC indeks

Bu Product uyarıları URL indeksini **engellemez**; yalnızca Product snippet / merchant listing uygunluğunu keser.

## Patch (kod yok — sonraki tur)

`Urunlerimiz/{Dis-Mekan-Led-Ekran,Ic-Mekan-Led-Ekran,Rental-Ekran}/urun-ozellikleri.html` ve `Urunlerimiz/Gob-Led-Ekran/{hero-banner.html,page.html}` içindeki Product JSON-LD bloklarını sil. FAQPage + BreadcrumbList’e dokunma.

## Validate

Canlı URL Inspection “Canlı URL’yi test et” → Product öğesi kaybolmalı; FAQ + Breadcrumb kalmalı.  
`python AGENT-HUB/check-product-pages-schema.py` (Product kalmamalı).
