# Visual-UX — Hero dikey yükseklik (2026-09-11)

**Kapsam:** Yalnızca `Anasayfa/Hero.html` `.ledajans-hero` min-height. Header dokunulmadı.

## Viewport Issues
- Seçilen `section.ledajans-hero.fair-reveal-active` ~520px kalmıştı; eski tam viewport yüksekliği geri alındı.

## Copy Fixes
- Metin değişmedi.

## Visual/A11y Fixes
- `min-height: max(600px, 100vh)` / `100dvh` geri yüklendi.
- Fuar kartı + sağ metin in-flow layout aynı.

## CSS Diff Özeti
```css
.ledajans-hero {
  min-height: max(600px, 100vh);
  min-height: max(600px, 100dvh);
}
```

## QA Checklist
- [x] `python scripts/deploy-homepage-hero.py` → widgets_updated=1, page_update=200, cache 200
- [x] Canlı: `max(600px, 100vh)` ve `100dvh` var
- [x] `ledajans-hero-poster` yok; SIGN logo ve fair-reveal-active duruyor
