# Visual-UX — Hero fuar kartı arka plan + SIGN wordmark (2026-09-21)

## Viewport Issues
- Desktop (~1440): fuar paneli solda; kart üstünde kırmızı SIGN wordmark 86px yükseklik, ortalı.
- Mobil (≤768): panel `display:none`; `::before` görsel yok (LCP). Wordmark CSS yedek duruyor.

## Copy Fixes
- SIGNISTANBUL düz yazı başlığı yoktu; üst görsel `alt="SIGN ISTANBUL"` ile wordmark oldu.
- Kart metni aynı: slogan, “LEDAJANS fuarda sizi bekliyor”, tarih/yer, HOL/STANT.
- Hero CTA “Hemen Arayın” + play butonu değişmedi.

## Visual/A11y Fixes
- Kart `::before` desktop: pinwheel WebP + koyu overlay (`opacity: 0.82`).
- SIGN PNG: siyah zemin şeffaf, sıkı kırp 192×74, WebP 2.5KB.
- Wordmark `drop-shadow` ile pinwheel üzerinde okunur; kontrast kart metin kutularında korundu.
- `aside` `aria-label="SIGNISTANBUL fuar duyurusu"` duruyor.

## CSS Diff Özeti
- `.ledajans-hero-fair-card::before` (min-width 769px): `signistanbul-fair-bg.webp` + linear-gradient overlay.
- `.ledajans-hero-fair-sign-logo`: height 86px, max-width 380px, center; wrap `justify-content:center`.
- img src: `signistanbul-sign-logo.webp` (width=192 height=74).

## QA Checklist
- [x] Canlı HTML: `signistanbul-fair-bg.webp` var, `Firefly_Gemini-Flash-8.png` yok
- [x] Canlı HTML: `signistanbul-sign-logo.webp` var, eski `signistanbul-logo-1.webp` yok
- [x] Playwright 1440×900: kırmızı SIGN üstte, pinwheel zemin, play + Hemen Arayın
- [x] `hero-widget` 404 → Elementor fallback page 1248, 1 widget, cache DELETE 200
- [x] Mobil patch: Firefly + yeni bg URL `str_ireplace` → `none`

## Medya
| Varlık | URL |
|---|---|
| Fuar kartı arka plan | https://ledajans.com/wp-content/uploads/2026/09/signistanbul-fair-bg.webp |
| SIGN wordmark | https://ledajans.com/wp-content/uploads/2026/09/signistanbul-sign-logo.webp |

---

## Pass 2 — Kart-içerik boşluğu + SIGN kesilme (2026-09-21)

### Viewport Issues
- Eski: `justify-content:flex-end` + `max-width:760px` + `margin-right` içeriği sağa itiyordu (~150px+ gap).
- Yeni: `flex-start` + `margin-left: kart + clamp(2.5rem,4vw,4.75rem)`.
- 1440: kart→H1 gap **74px**, overlap yok. 1280: **67px**, overlap yok.

### Copy Fixes
- Yok. Play + Hemen Arayın aynı.

### Visual/A11y Fixes
- SIGN: `object-fit:contain`, wrap `overflow:visible` + `flex-shrink:0` + `min-height:78px`, hover `translateY` kaldırıldı (üst kesilme).
- Logo 72px, natural 192×74, `logoClipped=false`.

### CSS Diff Özeti
- `.ledajans-hero.fair-reveal-active` → `justify-content:flex-start`; content `text-align:left` + `--fair-gap`.
- Mobil: margin-left 0, text-align center.

### QA Checklist
- [x] 1440/1280 screenshot: SIGN tam, gap 40–80, overlap yok, pinwheel + play + CTA
- [x] Deploy page 1248, cache DELETE 200, commit yok

---

## Pass 3 — Layout geri alındı (2026-09-21)

Son turdaki `flex-start` / `margin-left` / sola hizalama geri alındı. Pinwheel + SIGN URL korundu.

- Desktop: `justify-content:flex-end`; content `width: calc(100% - min(450px, 38vw) - 4.75rem); max-width:760px`
- Mobil: `flex-start`, width 100%
- Canlı 1440: kart left 29px, içerik left 613px, `flex-end` + `text-align:center`, SIGN + pinwheel + play + Hemen Arayın

