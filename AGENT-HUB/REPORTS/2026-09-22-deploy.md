# WordPress-Deploy — Anasayfa Widget 1 (Hero)

Tarih: 2026-09-22  
Kapsam: `Anasayfa/Hero.html` → anasayfa **page 1248** / Elementor HTML widget **`122e243`**.  
Komut: `python scripts/deploy-homepage-hero.py --dry-run` (repo kökü).  
Canlı yazma: **yapılmadı**. Secret / Application Password çıktıda yok.

## Dry-Run Sonuç

| Madde | Sonuç |
|---|---|
| Komut | `python scripts/deploy-homepage-hero.py --dry-run` |
| Çıktı | `DRY_RUN: yazilmadi` |
| Exit | **0** |
| WP POST / meta update | yok (`--dry-run` HTTP yazmadan çıkıyor) |
| Kimlik bilgisi | yazdırılmadı |

Yerel kapı (`Hero.html`): `<picture>` + `hero-stant-poster` var → script kabul etti.

Ek payload kontrol (rapor, script dışı):

| Kontrol | Durum |
|---|---|
| `hero-stant-1280.mp4` (`data-src`) | var |
| `hero-stant-poster.webp` + `hero-stant-poster-mobile.webp` | var |
| Ham `StantVideo.mp4` | **yok** |
| `Firefly-548225` | **yok** |
| Marker `ledajans-hero` | var |
| HTML boyutu | 50 452 byte |

Yerel medya: `hero-stant-1280.mp4` 311 557 B; poster 48 318 B; mobil poster 44 126 B.

Salt-okunur HEAD (yazma değil): üç URL **HTTP 200** (`/wp-content/uploads/2026/09/`).

Dry-run sınırı: canlı page 1248 / widget `122e243` **GET edilmedi**; hedef eşleşme ancak canlı koşuda doğrulanır.

## Değişecek Sayfalar

Canlı onay sonrası **yalnızca**:

| Hedef | Değer |
|---|---|
| Site | ledajans.com anasayfa |
| WP page ID | **1248** |
| Widget | Widget 1, Elementor HTML **`122e243`** |
| Kaynak | `Anasayfa/Hero.html` |
| Birincil yazma | `POST /wp-json/ledajans/v1/hero-widget` (`page_id=1248`, `widget_id=122e243`) |
| Fallback | `_elementor_data` içinde yalnızca `122e243` (`walk_replace`); diğer HTML widget yok |
| Cache (yalnızca canlı başarı) | Elementor Files & Data clear |

Dokunulmayacak: diğer anasayfa widget’ları, RankMath, mu-plugin (bu scriptte yok).

## Risk

| Seviye | Not |
|---|---|
| Orta | ~50 KB HTML widget tam değişimi; Elementor CSS/JS cache gecikmesi |
| Düşük | Fallback yalnızca `122e243` |
| Düşük | Medya 200 — kırık `data-src`/poster riski düşük |
| Bilgi | Dry-run WP’ye bağlanmıyor; 403 ancak canlıda görülür |
| Bilgi | Mobilde video CSS/JS kapalı; LCP poster |

## Go / No-Go

| Karar | |
|---|---|
| Dry-run (payload + yazmama) | **GO** |
| Canlı yazma (bu tur) | **NO-GO** — kullanıcı yasak + ajan kuralı (açık onay yok) |

Canlı için ayrı onay gerekir: `--dry-run` olmadan `python scripts/deploy-homepage-hero.py`.

## Rollback

Canlı yazılmadığı için rollback gerekmedi.

Canlı sonrası:

1. WP page **1248** revision → önceki `_elementor_data` / widget HTML.
2. Repo: `git show 80f00a2:Anasayfa/Hero.html` (atrium v2) → aynı script + onay.
3. Acil: widget `122e243` Elementor HTML’e son bilinen kopya; Elementor cache clear.

Yedek alınmadan canlı yazılmamalı (script otomatik HTML dump tutmuyor).
