# QA-Auditor — Anasayfa Widget 1 stant video

Tarih: 2026-09-22  
Kapsam: `Anasayfa/Hero.html` (Elementor `122e243` / page 1248). Canlı yazma yok (bu ajan).  
Karar: **PASS** (widget / link / schema / robots / diğer widget). Plugin senkronu uyarı.

## Pass / Fail

| | |
|---|---|
| **Sonuç** | **PASS** |
| Critical | yok |
| `[BLOCKER]` (bugünkü raporlar) | yok |

## Kanıt (taze)

| Kontrol | Sonuç |
|---|---|
| `python scripts/verify-mobile-perf-live.py` | **VERIFY_OK** exit 0 |
| Media HEAD | `hero-stant-1280.mp4` 200 / 311557 / `video/mp4`; poster 200 / 48318; poster-mobile 200 / 44126 |
| Ham `StantVideo.mp4` | **yok** (HEAD 301 → `/`) |
| Canlı desktop | stant mp4 var, Firefly yok, `preload=none`, reduced-motion var |
| Canlı mobil | video CSS `display:none`, stant poster-mobile, ham StantVideo yok, `as=video` yok |
| H1 | 1 adet: `LED Ekran Satış, Kiralama ve Kurulum` — CTA `tel:+902122204004` |
| Hero.json-ld | parse OK: Organization / WebSite / LocalBusiness |
| Canlı robots meta | `index, follow, max-snippet:-1, max-video-preview:-1, max-image-preview:large` |
| Canonical | `https://ledajans.com/` — noindex yok |
| `robots.txt` | 200; `Disallow: /` yok; sitemap var; son not 2026-05-15 |
| Hero.html | robots / canonical / noindex **yok** (widget dokunmamış) |
| Diğer anasayfa HTML widget | git **temiz**; canlı `ledajans-about` / `indoor` / `outdoor` duruyor |
| Deploy script | `walk_replace(..., 122e243)` — yalnızca Widget 1 |

Hero href: site + 2 poster (200), `#ledajans-video` (JS `preventDefault`), `tel:`, Maps, forms.gle. Fair bg / logo WebP-JPEG 200.

## Smoke

| Komut | Exit | Not |
|---|---|---|
| `python AGENT-HUB/auto_orchestrator.py` | **1** | Hardcoded `/workspace/...` (Windows; bu turla ilgili değil) |
| `python AGENT-HUB/live_dashboard.py` | **1** | Aynı `/workspace` path |
| `python deploy-to-wordpress.py --dry-run` | **0** | 36 sayfa, 0 hata; yazmadı |

## Warnings

1. **P1 — canlı mu-plugin eski:** mobil hâlâ `ldajsn2-mobile-q60.webp` `preload` + `fetchpriority=high` **ve** stant poster-mobile (çift LCP adayı). Repo 1.2.5 q60’ı stant poster’a çeviriyor; write-files 404 ile canlıya yazılamadı. Widget doğru; plugin değil.
2. Canlı 3× `ld+json`: iki aynı `@graph` (Hero + `homepage-schema`) + RankMath `CollectionPage`. Parse kırık değil; bu diff schema’ya dokunmadı.
3. `#ledajans-video` hedef id yok (önceden var; play JS ile modal).
4. `2026-09-22-risk-hero-video.md` / `deploy.md` / performans “VERIFY_FAIL beklenir” **stale** (medya artık 200, widget canlıda stant).

## Suggestions

- Mu-plugin 1.2.5’i ayrı onayla canlıya al; q60 high-preload kesilsin.
- `verify-mobile-perf-live.py` içine `ldajsn2-mobile-q60` yok kapısı.
- İleride Hero içindeki kopya JSON-LD’yi kaldır (yalnız `homepage-schema` widget) — bu turda yapma.
- Stale 2026-09-22 risk/deploy notunu güncelle.

## Re-test plan

1. Plugin canlı sonrası: mobil HTML’de q60 preload yok, stant poster-mobile tek high image preload.
2. `python scripts/verify-mobile-perf-live.py` + (isteğe bağlı) mobil Lighthouse LCP = stant poster, video request yok.
3. robots/canonical/noindex tekrar (beklenen: değişmez).
4. Widget 3–5 marker’lar duruyor mu.

## Risk-Guardian uyum

Yetkisiz push / ikinci canlı deploy: **RED**. Plugin güncellemesi ayrı onay ister.
