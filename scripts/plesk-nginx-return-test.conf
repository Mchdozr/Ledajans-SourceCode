# Plesk > Ek NGINX directives — rewrite ise ise bunu deneyin (tek URL test)
if ($request_uri ~ "^/led-ekran-nasil-secilir-rehber/?$") {
    return 301 https://$host/blog/led-ekran-nasil-secilir-rehber/;
}
