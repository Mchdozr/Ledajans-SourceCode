# Plesk > ledajans.com > Apache & nginx > Additional nginx directives
# www → apex 301 (Preferred domain zaten yapıyorsa eklemeyin)

if ($host ~* ^www\.ledajans\.com$) {
    return 301 https://ledajans.com$request_uri;
}
